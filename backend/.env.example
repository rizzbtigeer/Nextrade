PORT=5000
MONGO_URI=mongodb+srv://<utilisateur>:<motdepasse>@<cluster>.mongodb.net/gros-market?retryWrites=true&w=majority

# IMPORTANT : générer une chaîne longue et aléatoire en production, jamais ce texte tel quel.
# Exemple pour en générer une : node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=remplace_moi_par_une_longue_chaine_aleatoire

WHATSAPP_PHONE_NUMBER=237600000000
NODE_ENV=development

# URL(s) exacte(s) du site en production, séparées par une virgule si plusieurs.
# Utilisé par CORS pour n'accepter que les requêtes venant de ton propre site.
FRONTEND_URL=https://www.tonsite.com
